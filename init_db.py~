from app import app,db
from app.models import User

repo_table=''' create table repos_info (repo_name,repo_address,repo_name,repo_passwd,local_checkout_path,repo_type,remote_deploy_path)'''

def init_db_user():
    user=User('hoo@wikiki.cn','meitianhui')
    db.session.add(user)
    db.session.commit()


if __name__=="__main__":
    init_db_user()
