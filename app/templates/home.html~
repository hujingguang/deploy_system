{% extends "base.html"  %}
{% block title %}每天惠版本发布平台{%endblock%}
{% block page_content %}
    <div class="row">
        <div class="col-md-2">
            <div class="panel-group" id="accordion">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h4 class="panel-title">
                         <a data-toggle="collapse" data-parent="#accordion" href="#collapseOne" class="collapsed"><span class="glyphicon glyphicon-list-alt btn-xs"></span>版本系统管理</a>
                        </h4>
                    </div>
                    <div id="collapseOne" class="panel-collapse collapse in ">
                        <div class="panel-body">
                            <table class="table">
                                <tbody>
                                <tr><td><span class="glyphicon glyphicon-tree-conifer btx-xs"></span><a href="/add/repos">添加版本库</a></td></tr>
                                <tr><td><span class="glyphicon glyphicon-tree-conifer btx-xs"></span><a href="/list/repoinfo">版本库信息</a></td></tr>
                                <tr><td><span class="glyphicon glyphicon-tree-conifer btx-xs"></span><a href="/deploy">发布版本</a></td></tr>
                                <tr><td><span class="glyphicon glyphicon-tree-conifer btx-xs"></span><a href="/list/deployinfo">操作日志</a></td></tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-10">
            {% block form_content %}
            {%endblock%}
        </div>
    </div>
{%endblock%}
