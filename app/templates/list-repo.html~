{%extends "home.html" %}
{% block form_content %}
<div class="panel panel-default">
    <div class="panel-heading">
        <b>版本库信息</b>
    </div>
    <div class="panel-body">
      <div style="margin-left:10px;">
    {%if repos %}
        <div class='table-responsive'>
          <table class="table table-hover">
            <thead>
            <tr>
            <th>id</th>
            <th>Name</th>
            <th>RepoAddres</th>
            <th>Checkout</th>
            <th>Testing</th>
            <th>Online</th>
            <th>Type</th>
            </tr>
           </thead>
           <tbody>
               {% for repo in repos %}
                <tr>
                    <td>{{ loop.index  + (page-1) * per_page }} </td>
                    <td>{{ repo.repo_name }}</td>
                    <td>{{ repo.repo_address }}</td>
                    <td>{{ repo.local_checkout_path }}</td>
                    <td>{{ repo.test_deploy_path }}</td>
                    <td>{{ repo.remote_deploy_path }}</td>
                    <td>{{ repo.repo_type }} </td>
                </tr>
            {% endfor %}
          </tbody>
      </div>
    {%endif%}
     </div>
     {{pagination.links}}
   </div >
      <div class="input-group" style="width:30%;margin-left:300px;margin-top=100px">
        <form action="/list/repoinfo/search" method="post" >
              <span class="input-group-btn" style="margin-left:2px;">  
              <button type="submit" class="btn btn-primary" name="do_action" value="search" style="margin-left">search</button>
          </span>
          <input type="text" class="form-control" name="repo">
        </form>
       <span class="input-group-addon">库名</span>
      </div>
   </div>
{% endblock %}
