{%extends "home.html" %}
{% block form_content %}
<div class="panel panel-default">
    <div class="panel-heading">
        <b>代码发布信息</b>
    </div>
    <div class="panel-body">
      <div style="margin-left:10px;">
    {%if deploy %}
        <div class='table-responsive'>
          <table class="table table-hover">
            <thead>
            <tr class="info">
            <th>id</th>
            <th>Name</th>
            <th>Now_version</th>
            <th>Target</th>
            <th>Env</th>
            <th>Time</th>
            <th>Person</th>
            </tr>
           </thead>
           <tbody>
               {% for d in deploy %}
                <tr class="warning">
                    <td>{{ loop.index  + (page-1) * per_page }} </td>
                    <td>{{ d.repo_name }}</td>
                    <td>{{ d.now_version }}</td>
                    <td>{{ d.deploy_target }}</td>
                    <td>{{ d.deploy_env }}</td>
                    <td>{{ d.deploy_date }}</td>
                    <td>{{ d.deploy_person }} </td>
                </tr>
            {% endfor %}
          </tbody>
      </div>
    {%endif%}
     </div>
     {{pagination.links}}
   </div >
      <div class="input-group" style="width:40%;margin-left:300px;margin-top=100px">
        <form action="/list/repoinfo/search" method="post" >
          <span class="input-group-btn" style="margin-left:2px;">  
              <button type="submit" class="btn btn-primary" name="do_action" value="search" style="margin-left">search</button>
          </span>
          <input type="text" class="form-control" name="repo" style="margin-left">
          <span class="input-group-addon">库名</span>
          <span class="input-group-btn" style="margin-left:2px;"></span>
          <select class="form-control" style="margin-right:70px" data-style="btn-danger" name='envtype'>
              <option value="test">Test</option>
              <option value="online">Online</option>
           </select>
          <span class="input-group-addon">环境</span>
   </form>
      </div>
   </div>
{% endblock %}
